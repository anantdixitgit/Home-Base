<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HomeBase | Where Every Stay Feels Like Home</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Playfair+Display:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary: #ff5a5f;
        --secondary: #00a699;
        --light: #ffffff;
        --dark: #484848;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Montserrat", sans-serif;
        color: var(--light);
        overflow-x: hidden;
      }

      .hero-section {
        position: relative;
        width: 100%;
        height: 100vh;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 0 2rem;
      }

      .video-background {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        min-width: 100%;
        min-height: 100%;
        width: auto;
        height: auto;
        z-index: -1;
        filter: brightness(0.7);
      }

      .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
          135deg,
          rgba(0, 0, 0, 0.3) 0%,
          rgba(0, 0, 0, 0.1) 100%
        );
        z-index: -1;
      }

      .hero-content {
        max-width: 800px;
        margin-bottom: 3rem;
        opacity: 0;
        animation: fadeInUp 1s 0.5s forwards;
      }

      .hero-title {
        font-family: "Playfair Display", serif;
        font-size: 4rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        line-height: 1.2;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
      }

      .hero-subtitle {
        font-size: 1.4rem;
        font-weight: 400;
        margin-bottom: 2rem;
        line-height: 1.6;
        max-width: 700px;
        text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
      }

      .start-button {
        display: inline-block;
        padding: 1rem 2.5rem;
        font-size: 1.1rem;
        font-weight: 600;
        color: var(--light);
        background-color: var(--primary);
        border: none;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        text-decoration: none;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
        text-transform: uppercase;
        letter-spacing: 1px;
        opacity: 0;
        animation: fadeInUp 1s 0.8s forwards;
      }

      .start-button:hover {
        background-color: var(--secondary);
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
      }

      .start-button:active {
        transform: translateY(1px);
      }

      /* Animations */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Mobile Responsiveness */
      @media (max-width: 768px) {
        .hero-title {
          font-size: 2.5rem;
        }

        .hero-subtitle {
          font-size: 1.1rem;
        }

        .start-button {
          padding: 0.8rem 2rem;
          font-size: 1rem;
        }
      }

      @media (max-width: 480px) {
        .hero-title {
          font-size: 2rem;
        }

        .hero-subtitle {
          font-size: 1rem;
        }
      }

      .anant{
       
        font-weight :800;
      }
    </style>
  </head>
  <body>
    <section class="hero-section">
      <div class="hero-overlay"></div>
      <video autoplay muted loop class="video-background">
        <source src="/video/homebase2-video.mp4" type="video/mp4" />
        Your browser does not support the video tag.
      </video>

      <div class="hero-content">
        <h1 class="hero-title">Find Your Perfect Escape</h1>
        <p class="hero-subtitle">
          Discover handpicked homes and unique experiences around the world.
          Whether you're looking for a cozy cabin or a luxury villa, we've got
          the perfect stay waiting for you.
          <br />
          <p class="anant">CREATED BY ANANT DIXIT</p>
        </p>
      </div>

      <a href="/listings" class="start-button">Begin Your Journey</a>
    </section>
  </body>
</html>
